{
  "name": "spotlight-delaware-election-night-results",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "validate:headshots": "node scripts/validate-headshots.mjs",
    "smoke:source": "node scripts/smoke-source.mjs",
    "preview": "python3 -m http.server 4173 -d public",
    "worker:dev": "npx wrangler dev --config worker/wrangler.toml",
    "worker:deploy": "npx wrangler deploy --config worker/wrangler.toml"
  },
  "devDependencies": {
    "wrangler": "^4.37.1"
  }
}
